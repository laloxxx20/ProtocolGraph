Before compile you need to set the IP address of machine client in file main.cpp

# Compile
g++ main.cpp -o main

# Run Test
##Run Server **./main s**
##Run Client **./main c**, this can be executed again in another console if you want a new connection with server